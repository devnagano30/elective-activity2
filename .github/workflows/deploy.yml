name: Deploy to GitHub Pages

on:
  push:
    branches: [main]
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: pages
  cancel-in-progress: true

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm

      - run: npm ci

      # BASE_URL makes the generated site's asset paths work at
      # https://<user>.github.io/<repo>/ instead of the site root — required
      # for a GitHub Pages *project* page (not a username.github.io repo).
      # nuxt.config.ts reads it at build time; the NUXT_APP_BASE_URL runtime
      # override cannot be used here because it applies after the prerenderer
      # has mounted its routes, which yields "Redirecting..." stubs.
      - run: npm run generate
        env:
          BASE_URL: /${{ github.event.repository.name }}/

      - uses: actions/upload-pages-artifact@v3
        with:
          path: .output/public

  deploy:
    needs: build
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - id: deployment
        uses: actions/deploy-pages@v4
